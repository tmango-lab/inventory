-- ==========================================
-- SCRIPT: RESET DATA (KEEP CONFIG)
-- ใช้สำหรับล้างข้อมูลสินค้าและประวัติทั้งหมด แต่เก็บการตั้งค่าชั้นวาง (Shelf Config) ไว้
-- ==========================================

BEGIN;

-- 1. ลบประวัติการเบิกจ่ายทั้งหมดก่อน (เพราะผูกกับสินค้า)
DELETE FROM transactions;

-- 2. ลบสินค้าทั้งหมด
DELETE FROM products;

-- 3. (Optional) ลบไฟล์ภาพถ้ามีการเก็บใน Storage (ส่วนนี้ต้องลบมือในเมนู Storage ของ Supabase เอง)

COMMIT;

-- ปล. ตาราง shelf_configs จะไม่ถูกแตะต้อง ข้อมูลชั้นวางจะยังอยู่ครบ
